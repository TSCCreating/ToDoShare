<div data-type="todos">
  <div class="container">
    <% todos.each do |todo| %>
      <div class="todo-box">
        <div class="todo-wrapper">
          <%= button_to "icon", todo_path(todo), method: :delete, data: { confirm: 'Sure?' } %>
          <%= image_tag("todos/#{todo.twitter_id}/#{todo.images[0].name}") %>
          <div class="row lower">
            <button>・・・</button>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>